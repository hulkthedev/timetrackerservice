version: "3"
services:
  nginx-timetrackerservice:
    image: nginx:latest
    ports:
      - "3699:80"
    volumes:
      - .:/var/www/html
      - ./config/nginx/conf.d-default.conf:/etc/nginx/conf.d/default.conf
    links:
      - php-timetrackerservice

  php-timetrackerservice:
    image: php-timetrackerservice:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
#    links:
#      - mariadb-timetrackerservice

#  mariadb-timetrackerservice:
#    image: mariadb
#    environment:
#      MYSQL_ROOT_PASSWORD: admin
#      MYSQL_DATABASE: test
#    volumes:
#      - ./sql/migrate:/docker-entrypoint-initdb.d
#      - ./config/mariadb:/etc/mysql/conf.d